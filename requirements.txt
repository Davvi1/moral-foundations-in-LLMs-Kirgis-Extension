# Analysis, figures and the test suite. Runs on a laptop — no GPU, no CUDA.
#
# This is enough to reproduce EVERYTHING in results/derived/ and figures/ from
# results/raw/, and to run all 320 tests, EXCEPT the Bayesian variance-ratio fit
# (requirements-fit.txt) and the inference that produced results/raw/ in the first
# place (requirements-inference.txt).
#
# Versions are pinned to what the committed artifacts were produced with, because
# the point of this file is reproduction rather than a supported version range.
# Local analysis environment: Python 3.10.11 on Windows.
#
#   python -m pip install -r requirements.txt
#   pytest                                  # 320 tests, ~5 min
#
# transformers is here for the TESTS, not for inference: the harness is exercised
# against a fake vLLM driven by the real tokenizers, so a tokenizer claim that stops
# being true fails the suite on a laptop rather than after a GPU download.

pandas==2.3.1
numpy==2.2.6
scipy==1.15.3
matplotlib==3.10.9
PyYAML==6.0.2

# figures -> deck
python-pptx==1.0.2
pillow==11.3.0

# tests
pytest==8.4.1
transformers==5.14.1
truststore==0.10.4
